<nav class="navbar navbar-expand-lg bg-body-tertiary border-bottom border-body" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">KNews</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0"> 
        <li class="nav-item">
          <%= link_to 'Home', root_url, class: 'nav-link active' %>
        </li>
        <% if !user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Nova Conta', new_user_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>

      <ul class="navbar-nav ms-auto">
        <% if !user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Entrar', entrar_path, class: 'nav-link' %>
          </li>
        <% else %>
        
          <li class="dropdown">
            <a href="#" class="dropdown-toggle text-decoration-none text-light" data-bs-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
              Bem Vindo, <%= current_user.name %> <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to 'Minha Página', articles_path, class: 'dropdown-item' %>
              </li>
              <li>
                <%= link_to 'Editar', '#', class: 'dropdown-item' %>
              </li>
              <li class="divider" role="separator"></li>
              <li>
                <%= link_to 'Sair', sair_path, method: :delete, class: 'dropdown-item' %>
              </li>
            </ul>
          </li>
        <% end %>
      </ul>
      
    </div>
  </div>
</nav>